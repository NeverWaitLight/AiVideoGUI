# 角色物种设计功能更新

## 更新时间
2026-07-27

## 功能概述
为角色生成系统添加了物种设计字段，支持在 AI 生成角色时自动识别和标注角色的种族/物种类型（如：人类-白人、人类-黑人、人类-黄种人、动物、拟人化动物等）。

## 修改内容

### 1. AI 生成 Prompt 更新
**文件：** `service/text_model_service.py`

在 `generate_storyboard()` 方法的 system_prompt 中，为角色设计表的形象描述添加了 `[物种]` 标签要求：

```
形象描述格式模板：
[物种] 明确标注角色的种族/物种类型，必须从以下选项中选择一项：
       人类-白人、人类-黑人、人类-黄种人、人类-其他肤色、
       动物（注明具体种类如"橘猫"）、拟人化动物（注明具体种类如"拟人化兔子"）、
       虚拟生物（如"机器人""外星人"）等
[外貌] 年龄+性别+脸型+五官特征
[发型] 具体发型描述
...
```

**要求：**
- `[物种]` 标签必须放在第一位
- 物种分区是固定特征的一部分，跨视频保持一致
- AI 生成时会自动将物种信息注入到角色描述中

### 2. 固定特征标签更新
**文件：** `service/character_service.py`

更新了 `CharacterService._FIXED_TAGS` 常量，将"物种"添加到固定特征列表：

```python
# 修改前
_FIXED_TAGS = ("外貌", "发型", "发色", "瞳色", "体型")

# 修改后
_FIXED_TAGS = ("物种", "外貌", "发型", "发色", "瞳色", "体型")
```

**影响范围：**
- `extract_fixed_traits()` 方法会提取物种标签
- `enrich_prompt_with_characters()` 方法在生成视频时会注入物种信息

### 3. UI 占位符文本更新
**文件：** `ui/character_page.py`

更新了两处输入框的 placeholder 文本：
1. **角色编辑对话框**（`_CharacterEditDialog`）
2. **角色详情页**（`CharacterDetailPage`）

```python
"结构化形象描述，每行一个分区：\n"
"[物种] 人类-黄种人 / 人类-白人 / 人类-黑人 / 动物（橘猫）/ 拟人化动物（兔子）等\n"
"[外貌] 25岁女性，瓜子脸，柳叶眉\n"
...
```

### 4. 新增测试文件
**文件：** `tests/test_character_species_trait.py`

新增了 6 个测试用例，验证物种标签的提取功能：
- `test_extract_fixed_traits_with_species` - 测试人类-黄种人
- `test_extract_fixed_traits_animal_species` - 测试动物（橘猫）
- `test_extract_fixed_traits_anthropomorphic_species` - 测试拟人化动物（兔子）
- `test_extract_fixed_traits_white_human_species` - 测试人类-白人
- `test_extract_fixed_traits_black_human_species` - 测试人类-黑人
- `test_species_tag_is_first` - 验证物种标签在提取结果中排在第一位

## 使用示例

### AI 自动生成角色（从剧本提取）
当用户在分镜编辑器中点击"生成分镜"时，AI 会自动识别角色并生成带物种标签的描述：

```
| 角色名 | 引用代号 | 形象描述 |
| 李明 | CHAR_A | [物种] 人类-黄种人<br>[外貌] 35岁男性，国字脸，剑眉<br>[发型] 短发，侧分<br>[发色] 自然黑<br>[瞳色] 深棕色<br>[体型] 180cm，健壮体格 |
| 汤姆 | CHAR_B | [物种] 动物（橘猫）<br>[外貌] 圆脸，大眼睛<br>[发型] 短毛，毛茸茸<br>[发色] 橘色<br>[瞳色] 黄绿色<br>[体型] 中等体型 |
```

### 手动创建/编辑角色
用户在角色管理页面手动添加或编辑角色时，可以参考 placeholder 文本填写物种标签：

```
[物种] 拟人化动物（兔子）
[外貌] 30岁男性，长耳朵，粉色鼻子
[发型] 白色长毛，竖起的耳朵
[发色] 纯白色
[瞳色] 红色
[体型] 180cm，纤细挺拔
[上装] 黑色西装
[裤子] 黑色西裤
[鞋袜] 黑色皮鞋
[帽子] 黑色礼帽
```

## 视频生成时的自动注入
当分镜生成视频时，`CharacterService.enrich_prompt_with_characters()` 会自动将角色的固定特征（包括物种）注入到提示词中：

```
[角色形象] CHAR_A：人类-黄种人，35岁男性，国字脸，剑眉，短发，侧分，自然黑，深棕色，180cm，健壮体格
[画面] CHAR_A 推门进入审讯室，神情严肃
```

## 向后兼容性
- ✅ 旧数据兼容：没有 `[物种]` 标签的旧角色描述仍然可以正常工作
- ✅ 回退机制：`extract_fixed_traits()` 方法会检测结构化标签，若不存在则回退返回原始文本
- ✅ 测试覆盖：所有64个现有测试全部通过，无破坏性变更

## 测试结果
```
Ran 64 tests in 1.843s

OK
```

所有测试通过，包括：
- 11 个角色相关测试（新增 6 个物种标签测试）
- 53 个其他功能测试（全部通过，无回归）

## 注意事项
1. **物种标签必须放在第一位**：AI 生成时会优先处理物种信息，确保视频生成模型能正确理解角色类型
2. **固定特征的一部分**：物种标签属于固定特征，在不同视频中保持一致，不会因场景变化而改变
3. **服装与固定特征分离**：服装标签（上装/裤子/鞋袜/帽子）不是固定特征，可以根据场景自由变化

## 后续优化建议
1. 可以在 UI 中为物种标签添加下拉选择器，提供预设选项（人类-白人、人类-黑人、动物等）
2. 可以为不同物种类型提供专门的形象描述模板（如动物角色不需要"发型"标签，改为"毛发"）
3. 可以添加物种标签的可视化标识（如在角色卡片上显示物种图标）
